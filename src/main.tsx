import React from 'react';
import ReactDOM from 'react-dom/client';
import TimeConverterTool from './time-converter-tool';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TimeConverterTool />
  </React.StrictMode>
);